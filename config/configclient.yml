server:
  port: 8090
spring:
  profiles: test
  application:
    name: configclient

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8090/eureka